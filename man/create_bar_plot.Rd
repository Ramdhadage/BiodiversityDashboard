% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{create_bar_plot}
\alias{create_bar_plot}
\title{Create Interactive Bar Plot with echarts4r}
\usage{
create_bar_plot(
  data,
  x_axis_var = "event_month",
  y_axis_var = "n",
  xaxisLabel,
  yaxisLabel,
  title,
  flg_darkMode = FALSE
)
}
\arguments{
\item{data}{A data frame containing the data to be plotted.}

\item{x_axis_var}{x axis variable name from the data.}

\item{y_axis_var}{y axis variable name from the data.}

\item{xaxisLabel}{Character string specifying the label for the x-axis.}

\item{yaxisLabel}{Character string specifying the label for the y-axis.}

\item{title}{Character string specifying the title of the plot.}

\item{flg_darkMode}{Logical value (TRUE/FALSE) indicating whether to use dark mode styling.
Default is FALSE (light mode).}
}
\value{
An echarts4r visualization object that can be displayed in a Shiny app or R Markdown document.
}
\description{
This function creates an interactive bar chart visualization using the echarts4r library.
The visualization includes interactive features such as data zooming, tooltips, and export options.
It supports both light and dark themes through the \code{flg_darkMode} parameter.
}
\details{
The function provides several interactive features:
\itemize{
\item Data zooming through a slider at the bottom
\item Tooltips showing exact values on hover
\item Option to switch between bar and line representations
\item Export functionality for saving the chart as an image
\item Data view for examining the underlying data
}

The styling automatically adjusts based on the \code{flg_darkMode} parameter, with appropriate
color schemes for both light and dark modes.
}
\examples{
# Create sample data
data <- data.frame(
  event_month = month.abb[1:12],
  n = sample(50:100, 12)
)

# Create and display the bar plot
create_bar_plot(
  data = data,
  xaxisLabel = "Month",
  yaxisLabel = "Count",
  title = "Monthly Events",
  flg_darkMode = FALSE
)

}
